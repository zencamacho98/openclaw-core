# Frank Lloyd — Stage 1 Spec
# Review this document before approving.
# Approval does NOT authorize code generation.

build_id: "BUILD-039"
stage: 1
spec_version: "1.0"
generated_at: "2026-04-11T22:42:52.561509+00:00"

# ── Request ──────────────────────────────────────────────────────────────────
title: "Add API endpoint returns current portfolio"
description: "Add a new API endpoint that returns the current portfolio snapshot in JSON format."
success_criteria: "The new endpoint responds with a valid JSON object containing the current portfolio snapshot when accessed."

# ── Build classification ──────────────────────────────────────────────────────
build_type: new_service
risk_level: medium

# ── File set ──────────────────────────────────────────────────────────────────
affected_files:
  new:
    - path: "app/routes/portfolio_snapshot.py"
      purpose: "New API endpoint for returning current portfolio snapshot."
  modified:
    []

# ── Reuse ─────────────────────────────────────────────────────────────────────
reuse:
  capability_registry_checked: true
  decisions:
    - capability: "app/cost_warden.LMHelper"
      decision: not_needed
      reason: "No integration with cost warden is required for portfolio snapshot."

# ── Architecture ─────────────────────────────────────────────────────────────
architecture_layer: experience
layer_justification: "The new API endpoint will serve the frontend experience by providing portfolio data."

# ── Sentinel scope ────────────────────────────────────────────────────────────
sentinel_scope:
  test_file: "tests/test_portfolio_snapshot.py"
  coverage_note: "Test will ensure the portfolio snapshot endpoint returns valid JSON."

# ── Blast radius ──────────────────────────────────────────────────────────────
blast_radius:
  failure_mode: silent
  rollback: "Rollback will involve removing the new portfolio_snapshot.py file if issues arise."
  scope: "The change is isolated to the new portfolio snapshot service, affecting only that endpoint."
