# Frank Lloyd — Stage 1 Spec
# Review this document before approving.
# Approval does NOT authorize code generation.

build_id: "BUILD-056"
stage: 1
spec_version: "1.0"
generated_at: "2026-04-11T23:45:09.986593+00:00"

# ── Request ──────────────────────────────────────────────────────────────────
title: "Create API endpoint returns current portfolio"
description: "Create a new API endpoint that returns the current portfolio snapshot in JSON format."
success_criteria: "The new endpoint returns a valid JSON response containing the current portfolio snapshot when accessed."

# ── Build classification ──────────────────────────────────────────────────────
build_type: new_service
risk_level: medium

# ── File set ──────────────────────────────────────────────────────────────────
affected_files:
  new:
    - path: "app/routes/portfolio_snapshot.py"
      purpose: "New API endpoint to return current portfolio snapshot in JSON format."
  modified:
    []

# ── Reuse ─────────────────────────────────────────────────────────────────────
reuse:
  capability_registry_checked: true
  decisions:
    - capability: "app/cost_warden.LMHelper"
      decision: not_needed
      reason: "No integration with cost warden required."
    - capability: "observability/agent_state.py"
      decision: not_needed
      reason: "Agent state not relevant for portfolio snapshot."
    - capability: "observability/event_log.py"
      decision: not_needed
      reason: "No event logging required for this endpoint."
    - capability: "observability/telemetry.py"
      decision: not_needed
      reason: "Telemetry not required for this endpoint."
    - capability: "research/approval_policy.py"
      decision: not_needed
      reason: "No approval flow needed for a simple GET endpoint."
    - capability: "research/governance.py"
      decision: not_needed
      reason: "No governance checks required for this endpoint."

# ── Architecture ─────────────────────────────────────────────────────────────
architecture_layer: experience
layer_justification: "The new endpoint is part of the user-facing API that provides portfolio information."

# ── Sentinel scope ────────────────────────────────────────────────────────────
sentinel_scope:
  test_file: "tests/test_portfolio_snapshot.py"
  coverage_note: "Test file will verify the correct JSON response from the new endpoint."

# ── Blast radius ──────────────────────────────────────────────────────────────
blast_radius:
  failure_mode: silent
  rollback: "Rollback to previous state by removing the new endpoint file."
  scope: "Limited to the new portfolio snapshot endpoint without affecting existing services."
