# Frank Lloyd — Stage 1 Spec
# Review this document before approving.
# Approval does NOT authorize code generation.

build_id: "BUILD-004"
stage: 1
spec_version: "1.0"
generated_at: "2026-04-11T11:38:17.514972+00:00"

# ── Request ──────────────────────────────────────────────────────────────────
title: "Create API endpoint provides current portfolio"
description: "Create a new API endpoint that provides the current portfolio snapshot in JSON format."
success_criteria: "The new endpoint returns a valid JSON response containing the current portfolio snapshot when accessed."

# ── Build classification ──────────────────────────────────────────────────────
build_type: new_service
risk_level: medium

# ── File set ──────────────────────────────────────────────────────────────────
affected_files:
  new:
    - path: "app/routes/portfolio_snapshot.py"
      purpose: "New API endpoint to provide current portfolio snapshot in JSON format."
  modified:
    []

# ── Reuse ─────────────────────────────────────────────────────────────────────
reuse:
  capability_registry_checked: true
  decisions:
    - capability: "app/cost_warden.LMHelper"
      decision: not_needed
      reason: "No integration with cost warden functionality required."
    - capability: "observability/agent_state.py"
      decision: not_needed
      reason: "Agent state is not relevant for the portfolio snapshot."
    - capability: "observability/event_log.py"
      decision: not_needed
      reason: "No event logging required for this simple endpoint."
    - capability: "observability/telemetry.py"
      decision: not_needed
      reason: "Telemetry not necessary for this endpoint."
    - capability: "research/approval_policy.py"
      decision: not_needed
      reason: "No approval process needed for this simple endpoint."
    - capability: "research/governance.py"
      decision: not_needed
      reason: "Governance checks not applicable for this endpoint."

# ── Architecture ─────────────────────────────────────────────────────────────
architecture_layer: executive_control
layer_justification: "The endpoint will provide access to portfolio data, which is part of the executive control layer's responsibilities."

# ── Sentinel scope ────────────────────────────────────────────────────────────
sentinel_scope:
  test_file: "tests/test_portfolio_snapshot.py"
  coverage_note: "The test will validate the JSON response structure and content of the portfolio snapshot."

# ── Blast radius ──────────────────────────────────────────────────────────────
blast_radius:
  failure_mode: silent
  rollback: "Rollback will involve removing the new endpoint file if issues arise."
  scope: "The scope is limited to the new endpoint and does not affect existing services."
