# Frank Lloyd — Stage 1 Spec
# Review this document before approving.
# Approval does NOT authorize code generation.

build_id: "BUILD-011"
stage: 1
spec_version: "1.0"
generated_at: "2026-04-11T12:23:46.593993+00:00"

# ── Request ──────────────────────────────────────────────────────────────────
title: "Create API endpoint retrieves returns current"
description: "Create a new API endpoint that retrieves and returns the current portfolio snapshot in JSON format."
success_criteria: "The new endpoint responds with the correct portfolio snapshot data in JSON format when accessed."

# ── Build classification ──────────────────────────────────────────────────────
build_type: new_service
risk_level: medium

# ── File set ──────────────────────────────────────────────────────────────────
affected_files:
  new:
    - path: "app/routes/portfolio_snapshot.py"
      purpose: "New API endpoint to retrieve the current portfolio snapshot in JSON format."
  modified:
    []

# ── Reuse ─────────────────────────────────────────────────────────────────────
reuse:
  capability_registry_checked: true
  decisions:
    - capability: "app/cost_warden.LMHelper"
      decision: not_needed
      reason: "The new endpoint does not require cost warden functionalities."
    - capability: "observability/agent_state.py"
      decision: not_needed
      reason: "The new endpoint does not involve agent state management."
    - capability: "observability/event_log.py"
      decision: not_needed
      reason: "Event logging is not required for this isolated endpoint."
    - capability: "observability/telemetry.py"
      decision: not_needed
      reason: "Telemetry is not required for this isolated endpoint."
    - capability: "research/approval_policy.py"
      decision: not_needed
      reason: "Approval flow is not necessary for this new endpoint."
    - capability: "research/governance.py"
      decision: not_needed
      reason: "Governance checks are not applicable to this isolated endpoint."

# ── Architecture ─────────────────────────────────────────────────────────────
architecture_layer: experience
layer_justification: "The new API endpoint will serve the frontend experience by providing portfolio data."

# ── Sentinel scope ────────────────────────────────────────────────────────────
sentinel_scope:
  test_file: "tests/test_portfolio_snapshot.py"
  coverage_note: "The test will cover the new endpoint's functionality to ensure it returns the correct JSON response."

# ── Blast radius ──────────────────────────────────────────────────────────────
blast_radius:
  failure_mode: silent
  rollback: "Rollback will involve removing the new endpoint file without affecting existing services."
  scope: "The scope is limited to the new endpoint and does not impact other components."
