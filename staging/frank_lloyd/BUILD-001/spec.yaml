# Frank Lloyd — Stage 1 Spec
# Review this document before approving.
# Approval does NOT authorize code generation.

build_id: "BUILD-001"
stage: 1
spec_version: "1.0"
generated_at: "2026-04-11T07:52:05.960936+00:00"

# ── Request ──────────────────────────────────────────────────────────────────
title: "Peter, have Frank Lloyd fix Belfort’s"
description: "Peter, have Frank Lloyd fix Belfort’s stopping-state UI so the control buttons reflect stopping correctly. Success: when Belfort is stopping, the control buttons visibly reflect stopping and do not look like normal active-trading controls."
success_criteria: "when Belfort is stopping, the control buttons visibly reflect stopping and do not look like normal active-trading controls."

# ── Build classification ──────────────────────────────────────────────────────
build_type: modification
risk_level: critical

# ── File set ──────────────────────────────────────────────────────────────────
affected_files:
  new:
    []
  modified:
    - path: "app/routes/neighborhood.py"
      change: "Modify the stopping-state UI to ensure control buttons visually reflect the stopping state."

# ── Reuse ─────────────────────────────────────────────────────────────────────
reuse:
  capability_registry_checked: true
  decisions:
    - capability: "app/cost_warden.LMHelper"
      decision: not_needed
      reason: "No cost-related functionality is affected."
    - capability: "observability/agent_state.py"
      decision: not_needed
      reason: "No changes to agent state are required."
    - capability: "observability/event_log.py"
      decision: not_needed
      reason: "No logging changes are necessary."
    - capability: "observability/telemetry.py"
      decision: not_needed
      reason: "Telemetry is not impacted by UI changes."
    - capability: "research/approval_policy.py"
      decision: not_needed
      reason: "No approval flow changes are required."
    - capability: "research/governance.py"
      decision: not_needed
      reason: "Governance remains unchanged."

# ── Architecture ─────────────────────────────────────────────────────────────
architecture_layer: experience
layer_justification: "The modification involves changes to the user interface that users directly interact with."

# ── Sentinel scope ────────────────────────────────────────────────────────────
sentinel_scope:
  test_file: "tests/test_belfort_ui.py"
  coverage_note: "Test coverage will ensure the UI reflects the stopping state correctly."

# ── Blast radius ──────────────────────────────────────────────────────────────
blast_radius:
  failure_mode: loud
  rollback: "If the modification fails, the previous UI state will be restored."
  scope: "The scope is limited to the UI components related to the stopping state."

# ── Validation corrections applied ───────────────────────────────────────────
# These fields were corrected by deterministic rules after LM draft generation.
# Review these corrections — they indicate LM output that conflicted with doctrine.
validation_corrections:
  - field: risk_level
    original: "high"
    corrected: "critical"
    rule: "LM assessed 'high' but deterministic floor is 'critical' (critical file in change set: app/routes/neighborhood.py)"
