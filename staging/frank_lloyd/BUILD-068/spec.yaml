# Frank Lloyd — Stage 1 Spec
# Review this document before approving.
# Approval does NOT authorize code generation.

build_id: "BUILD-068"
stage: 1
spec_version: "1.0"
generated_at: "2026-04-11T23:45:33.237200+00:00"

# ── Request ──────────────────────────────────────────────────────────────────
title: "Create API endpoint returns current portfolio"
description: "Create a new API endpoint that returns the current portfolio snapshot in JSON format."
success_criteria: "The new endpoint responds with a valid JSON object containing the current portfolio snapshot when accessed."

# ── Build classification ──────────────────────────────────────────────────────
build_type: new_service
risk_level: medium

# ── File set ──────────────────────────────────────────────────────────────────
affected_files:
  new:
    - path: "app/routes/portfolio_snapshot.py"
      purpose: "New API endpoint to return the current portfolio snapshot in JSON format."
  modified:
    []

# ── Reuse ─────────────────────────────────────────────────────────────────────
reuse:
  capability_registry_checked: true
  decisions:
    - capability: "app/cost_warden.LMHelper"
      decision: not_needed
      reason: "The new endpoint does not require cost management functionalities."
    - capability: "observability/agent_state.py"
      decision: not_needed
      reason: "The new endpoint does not require agent state management."
    - capability: "observability/event_log.py"
      decision: not_needed
      reason: "The new endpoint does not require event logging."
    - capability: "observability/telemetry.py"
      decision: not_needed
      reason: "The new endpoint does not require telemetry functionalities."
    - capability: "research/approval_policy.py"
      decision: not_needed
      reason: "The new endpoint does not require approval flow."
    - capability: "research/governance.py"
      decision: not_needed
      reason: "The new endpoint does not require governance functionalities."

# ── Architecture ─────────────────────────────────────────────────────────────
architecture_layer: experience
layer_justification: "The new endpoint is part of the user-facing API that provides portfolio information."

# ── Sentinel scope ────────────────────────────────────────────────────────────
sentinel_scope:
  test_file: "tests/test_portfolio_snapshot.py"
  coverage_note: "The test file will validate the response format and data integrity of the new endpoint."

# ── Blast radius ──────────────────────────────────────────────────────────────
blast_radius:
  failure_mode: silent
  rollback: "No rollback required as it is a new service."
  scope: "The new service will only affect the portfolio snapshot endpoint."
