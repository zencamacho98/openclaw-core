# Frank Lloyd — Stage 1 Spec
# Review this document before approving.
# Approval does NOT authorize code generation.

build_id: "BUILD-071"
stage: 1
spec_version: "1.0"
generated_at: "2026-04-12T00:33:18.243813+00:00"

# ── Request ──────────────────────────────────────────────────────────────────
title: "Create API endpoint returns current portfolio"
description: "Create a new API endpoint that returns the current portfolio snapshot in JSON format."
success_criteria: "The new endpoint successfully returns the current portfolio snapshot in JSON when accessed, with the correct data structure and values."

# ── Build classification ──────────────────────────────────────────────────────
build_type: new_service
risk_level: medium

# ── File set ──────────────────────────────────────────────────────────────────
affected_files:
  new:
    - path: "app/routes/portfolio_snapshot.py"
      purpose: "New API endpoint to return current portfolio snapshot in JSON format."
  modified:
    []

# ── Reuse ─────────────────────────────────────────────────────────────────────
reuse:
  capability_registry_checked: true
  decisions:
    - capability: "app/cost_warden.LMHelper"
      decision: not_needed
      reason: "No integration with cost warden required for this endpoint."

# ── Architecture ─────────────────────────────────────────────────────────────
architecture_layer: experience
layer_justification: "The new API endpoint will directly serve the frontend by providing portfolio data."

# ── Sentinel scope ────────────────────────────────────────────────────────────
sentinel_scope:
  test_file: "tests/test_portfolio_snapshot.py"
  coverage_note: "Test will cover the functionality of the new portfolio snapshot endpoint."

# ── Blast radius ──────────────────────────────────────────────────────────────
blast_radius:
  failure_mode: silent
  rollback: "The rollback will involve removing the new endpoint file if issues arise."
  scope: "The scope is limited to the new endpoint and does not affect existing services."
