# research/ — autonomous research loop modules
#
# governance.py   — approved experiment classes, param ranges, manifest validation
# manifest.py     — ExperimentManifest dataclass
# generator.py    — bounded candidate generation
# batch_runner.py — in-process validation wrapper
# reviewer.py     — post-run diagnostic extraction
# ledger.py       — persistent research ledger (JSONL + markdown)
